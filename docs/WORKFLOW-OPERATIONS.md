# DetailAI 업무 프로세스 — 기획 문서(PDF) ↔ 플랫폼 반영 대조

> **기준**: 작업프로세스_AI상세페이지.pdf (0~8단계 + 디자이너/관리자 작업화면) · 대조일 2026-07-22 (코드 실사)
> 표기: ✅ 반영 · 🔶 부분 반영 · ❌ 미반영

---

## ① 프로세스 0~8단계 ↔ 플랫폼 매핑

| # | PDF 정의 | 담당 | 플랫폼 구현(상태·화면) | 반영 |
|---|---|---|---|---|
| **0** | 메일 발송 — 작성 링크 전달·제품 발송 안내·**문자 자동발송**·1주 미작성 시 재발송·3주 소요/반송 여부/AI 활용 고지 | 관리자 | 계정 생성(/users) + 크론 의뢰서 리마인더(메일+SMS). **초대(작성 링크) 발송 버튼·안내 고지 문구 없음** | 🔶 |
| **1** | 정보입력 — 로그인(사업자명+사업자번호 뒷5자리) 후 의뢰서 작성 | 기업 | /intake 4스텝 폼 + 임시저장, 로그인 방식 동일 (`invited→intake_submitted`) | ✅ |
| **2** | 입력정보확인 — 충분/불충분 판정, 불충분 시 **자동안내메일+유선**, 재작성 태그, **2차 보완은 무조건 유선**, **제품수령확인 체크** | 관리자 | **의뢰서 검토·보완요청 단계 자체가 없음** — 제출 즉시 스크립트 단계로. 제품수령 체크박스 없음 | ❌ |
| **2-1** | 포토그래퍼 — 수령 체크 시 구글시트 리스트업 자동화 → 촬영 → 드라이브 업로드 → **시스템 자동반영**. 패키지/파우치/본품 각도별 누끼(AI 합성) | 포토그래퍼 | **미구현** — 현재는 기업 업로드 제품사진을 누끼·레퍼런스로 사용(관리자 수동 업로드 병행 가능) | ❌ |
| **3** | 스크립트·스타일링 확인 — 각각 충분/불충분(AI 재작업/관리자 수정) → **디자이너 배정**. 불만족 시 **작업별 재실행 버튼** | 관리자 | `script_review` 검수 패널(승인/AI 재생성) + `design_plan_review` 기획 승인 + AssignPanel(배정) + **/pipeline/[id] ①~⑤ 재실행** | ✅ |
| **4-1** | 디자이너 AI초안 확인 — 만족(업글 진행)/불만족(관리자 협의·**AI 재작업 Max 2회**·프롬프트 개선). 스크립트·스타일링 기초자료 제공 | 디자이너 | /designer/[id] — AI초안 미리보기(/draft)·Figma 임포트·재생성 버튼. **재작업 2회 상한 카운트 없음** | 🔶 |
| **4-2** | 관리자 AI초안 확인(볼 수만, 디자이너 권한) | 관리자 | admin 모드 열람 + 콘솔(단, 관리자도 재실행 가능 — PDF보다 권한 넓음) | ✅ |
| **5** | 1차시안 업로드 — 폴더 자동생성, **PSD·JPG 업로드**+제출 → 관리자 확인으로 이동 | 디자이너 | SendDraftPanel(이미지 업로드=preview_url) + 최종본 제출(DeliveryPanel). **PSD 슬롯·폴더 자동생성 없음**(파일 형식 제한적) | 🔶 |
| **6** | 관리자 1차시안 확인 — 만족 시 **HTML 메일 자동발송**/불만족 시 보완 태그+구두협의 | 관리자 | /dashboard/review 시안보드(만족✓/불만족=revise 태그) + 전달 시 메일+SMS 자동발송 | ✅ |
| **7** | 기업 1차시안 확인 — 만족/불만족(수정보완 의견). 원칙: 텍스트 수정 가능·큰 수정은 유선. **7일 무회신 = 수정 없음 간주 후 파일 전달** | 기업 | /projects/[id] 초안 검토 + 코멘트 + 수정요청(2회 제한) + 무회신 리마인더 크론. **7일 자동 간주·자동 전달 없음** | 🔶 |
| **8** | 수정요구 확인 — 텍스트=자체 해결 / 큰 수정=디자이너 재작업 분류·구두협의 / 원본파일 전달 | 관리자 | revision_1/2 → designer_working 재작업 플로우 + 완료 시 ZIP 다운로드. 텍스트/큰수정 **분류 태그 없음**(전부 디자이너 재작업 경로) | 🔶 |

---

## ② 디자이너 작업화면 대조 (PDF p.2 ↔ /my-work)

| PDF 정의 | 구현 | 반영 |
|---|---|---|
| 4컬럼: 작업배정 → 작업중 → 1차시안 → 완료 (~건 카운트) | /my-work 4컬럼 칸반(시작 전→작업 중→1차시안→완료) + 건수 | ✅ |
| 카드 **D-day 카운트다운**("기업명(-14일)") | 카드에 D-day 없음(생성일 기준) | ❌ |
| **Hold** 태그 — 불만족 해결까지 최상단 점유 | 없음 | ❌ |
| **보완**(관리자 불만족) 태그 + 최상단 점유 | revise 태그 있음 · 최상단 정렬 없음 | 🔶 |
| **수정1/수정2**(기업 불만족) 태그 + 최상단 점유 | revision_1/2 상태로 구분(태그 UI·정렬 없음) | 🔶 |
| **검토중**(기업 피드백 대기) 표시 | 없음(1차시안 컬럼에 뭉침) | ❌ |
| 작업중: 폴더 자동생성 + PSD·JPG 업로드 → 제출 | 이미지 업로드만(위 5번과 동일) | 🔶 |

## ③ 관리자 작업화면 대조 (PDF p.3–4 ↔ /dashboard·/dashboard/review)

| PDF 정의 | 구현 | 반영 |
|---|---|---|
| 7컬럼 흐름: **메일전송 → 작업의뢰서** → 스크립트/스타일링 → AI초안 → 1차시안 → 기업 수정요청 → 완료 | /dashboard 칸반(접수~AI초안) + /dashboard/review(시안·수정·완료). **메일전송·작업의뢰서(보완/재작성/수령·미수령) 컬럼 없음** | 🔶 |
| **제품 도착 + 의뢰서 만족 → D-14 카운팅 시작**, 카드에 D-day | 없음(지연 필터 7/14/30일로 대체) | ❌ |
| 보완/재작성/수령/미수령/수정1완료 등 **태그 체계** + 최상단 점유 | revise 태그 1종만 | ❌ |
| 불만족 시 작업별 재실행 버튼 | /pipeline/[id] ①~⑤ | ✅ |
| AI초안 단계는 디자이너 권한(관리자는 열람) | 반영(관리자는 콘솔 실행도 가능) | ✅ |
| **디자이너별 작업현황 대시보드** | 담당자 필터로 부분 대체 | 🔶 |
| 1차시안 만족 시 메일/문자 자동발송("7일 이내 회신…") | 메일+SMS 자동발송 ✅ · **7일 고지 문구·자동 간주 없음** | 🔶 |
| 완료 시 자동메일(**PSD**, JPG, 스타일링 이미지 다운로드) | 납품 메일+SMS·HTML에서 ZIP 다운로드(모바일/PC/디자이너). PSD는 산출물에 없음 | 🔶 |

---

## ④ 미반영 갭 우선순위 (구현 결정 필요)

| 순위 | 갭 | 이유 |
|---|---|---|
| **P1** | **관리자 입력정보확인 단계** (충분/불충분 판정 → 보완 자동메일·문자 + 재작성 태그 + 제품수령 체크) | 프로세스의 관문 — 이게 없으면 부실 의뢰서가 그대로 파이프라인 진입. D-14 카운팅의 시작점이기도 함 |
| **P1** | **D-day 카운팅**(제품도착+의뢰서 만족 → 14일) + 카드 표시 + 지연 정렬 | 200개 병렬 운영의 일정 관리 축 |
| **P2** | **태그 체계**(Hold/보완/수정1·2/검토중/재작성/수령) + 불만족 건 최상단 점유 정렬 | 운영자 우선순위 파악 — 현재 revise 1종뿐 |
| **P2** | 메일전송·작업의뢰서 **칸반 컬럼** (관리자 화면 앞단) | 초대~접수 구간이 대시보드 사각 |
| **P3** | 7일 무회신 = 수정 없음 간주 → 자동 파일 전달 | 크론 확장으로 구현 가능(리마인더는 이미 있음) |
| **P3** | 포토그래퍼 자동화(구글시트·드라이브 연동) | 현재 기업 업로드로 대체 운영 중 — 실물 촬영 도입 시점에 결정 |
| **P3** | 1차시안 PSD 업로드 슬롯 · AI 재작업 2회 카운트 · 텍스트/큰수정 분류 | 운영 규칙의 시스템화 |

## ⑤ 이미 반영돼 있는 핵심 (재확인)

- 폐쇄형 로그인(사업자명+뒷5자리)·1회 제한 · 의뢰서 4스텝+임시저장
- 스크립트/기획 2중 검수 게이트 + 디자이너 배정 + 단계별 재실행 콘솔
- 디자이너 4컬럼 칸반·AI초안 확인·사업자 전달 · 시안보드(만족/불만족)
- 수정요청 2회 제한 · 초안 전달/납품 **메일+SMS 자동발송** · 리마인더 크론 2종
- 오늘(7/22) 수정: 승인불가 오류·revision 초안 실종·소스노출·캐시·목록누락·전이실패·재시도·my-work 액션 8건

**계정**: 관리자 admin/1234 · 디자이너 designer1~4@detailai.app/1234 · 사업자 사업자명+사업자번호 뒷5자리
**알림 계정**: 메일 hanapoweronteam@gmail.com(Gmail SMTP) · 문자 Solapi(발신 010-4915-5282)
