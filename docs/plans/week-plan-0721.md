# 주간 마무리 계획 (2026-07-21 ~ 07-25)

목표: 이번 주 안에 **① 상세페이지 룰 완전 강제 ② 플랫폼 MVP 운영 가능** 두 축을 끝낸다.
기준일 2026-07-20(월) 작업 종료 시점. 커밋 23건.

---

## 1. 오늘(7/20) 완료된 것

### 상세페이지 시스템
| 항목 | 결과 |
|---|---|
| 룰 전수 감사 | 룰 38개의 강제 수준 확정 — 코드 강제 21 / 프롬프트 10 / 미구현 7, 충돌 12건 식별 |
| 히어로 제품 노출(I1) | 규칙이 **한 번도 적용된 적 없는 죽은 경로**였음을 발견·복구. 재생성 컷에서 제품 완전 노출 확인 |
| 본문 폰트 하한(F4) | vw·clamp·baseCss 3중 누수 봉쇄 — 미달 10개 → **0개** |
| 폰트 역할 고정(F2·F3) | 타이틀 1종·서브 30px 렌더 계층 강제 |
| 제공 폰트 계약(F6) | 프리셋 4종을 클라이언트 폰트로 교체(본문 SUIT 고정 = F5 동시 충족) |
| 이미지 회수율(I6) | 원본 재배치 허용 + 4순위 폴백 → 6장 → 9장 |
| 시각 감사 | **장식 SVG가 있으면 감사 전체가 죽던 버그** 발견·수정(그동안 무검증 산출) |
| 자동 채점기 | `check-rules.ts` 신설 — 16항목 1분 채점. 현재 13/16 |
| 컷 재생성 도구 | `regen-styling-shots.ts` — 기획 재실행 없이 컷만 복구(재시도 3회) |

### 플랫폼
- S1(상태·태그·D-day·배정이력·발송로그) DB 적용·검증 완료
- S2(관리자 칸반 2종·디자이너 보드·기업 수정요청) 완료, Rubric 4/4/4/4
- 로그인 간소화(admin·designer1~4 / 1234), 기업 이메일 로그인 제거
- 기획자 역할 완전 폐지 — 담당자는 디자이너 하나로 통합
- **프로덕션 배포 + 3역할 실측 검증 완료** (https://detail-page-saas.vercel.app)
- 실제 7개 기업을 접수 단계·미배정으로 초기화 — 파이프라인 실전 테스트 준비 완료

---

## 2. 화요일(7/21) — 룰 강제 완결

### 오전
1. **씬 높이(S2) 검증** — 재측정된 변형 높이(594개/실측 55건)로 기획을 다시 돌려
   씬 7개가 1,600~2,400px에 들어오는지 확인. 오늘 네트워크 오류로 못 본 항목.
2. **룰 게이트화** — 지금 구조의 핵심 결함 해소:
   현재는 위반을 검출해도 기록만 남고 산출물이 그대로 나간다.
   → rule-check 위반 시 자동 재조립(최대 2회), 잔존 시 프로젝트에 `보완` 태그를 붙여
     관리자 시안 보드 최상단에 노출. "룰 = 산출 조건"으로 전환.
3. 결과물 채점(목표 16/16) + 씬별 화면 검토 → **클라이언트 공유본 갱신**

### 오후
4. **프롬프트에만 있는 룰의 코드화** (감사에서 확인된 10건 중 영향 큰 것)
   - I2 원료·성분 대표 2~4컷 — 청사진 검증에 컷 수 하한
   - V4 전 섹션 제품 잘림 — 시각 감사 항목 → 결정적 검사로 승격 검토
   - S7·V11 씬7 서사(재강조→구매유도) — 갭 기록 → 결정적 수리
   - S5 씬 내부 여백 리듬 120~180px

---

## 3. 수요일(7/22) — 이미지·피그마

1. **이미지 회수율 추가 개선** — 현재 9/17. 배열 아이템 슬롯(스텝·리스트) 주입 조건 완화가 핵심.
2. **I3 단색 바탕 → 누끼** 판정 — 태거에 배경 단순도 분류 추가(미구현 룰 중 마지막 이미지 항목).
3. **피그마 편집 경로 완성**
   - `/draft` 임포트용 변환: CSS 변수 평면화 + 장식·이미지·텍스트 레이어 이름 부여
   - 실제 html.to.design 임포트 테스트(디자이너 계정 필요) → 레이어 구조·폰트 대체 확인
4. 7사 배치 생성으로 다양성 확인(룰 강제가 다른 카테고리에서도 성립하는지)

---

## 4. 목요일(7/23) — 플랫폼 S3 (자동화)

**선결 조건: 사용자 준비물** (아래 5절)
1. 메일 발송 — Gmail SMTP 전환(현재 Resend 코드 → 프로바이더 추상화)
2. 문자/알림톡 — 솔라피 연동. 알림톡 미승인 시 SMS 폴백으로 시작
3. 크론 2종 — 의뢰서 1주 미작성 재발송 / 기업 7일 무회신 승인 간주
4. 발송 로그(notifications) 연동 — 중복 발송 방지
5. 완료 시 PSD·JPG 자동 발송 + deliverables 버킷

---

## 5. 금요일(7/24) — 리허설·마감

1. **3역할 E2E 리허설** — 초대 → 의뢰서 → 스크립트 → 배정 → 초안 → 1차시안 → 기업 확인 → 수정 → 완료 전 구간
2. 보안 점검(버킷 권한·RLS·이미지 보호)
3. 최종 배포 + 클라이언트 시연 자료 갱신
4. 예비: 7/25 버퍼

---

## 6. 사용자·클라이언트 액션 (막히면 일정 지연)

| 항목 | 필요 시점 | 사유 |
|---|---|---|
| **본문·서브 폰트 추가 제공** | 화요일 | 받은 15계열이 전부 타이틀용. 본문 가능한 건 SUIT·나눔스퀘어뿐이라 F6를 완전히 지키면 선택지가 없다 |
| **Gmail 앱 비밀번호** | 목요일 전 | 2단계 인증 후 발급 → 메일 자동 발송의 선결 조건 |
| **솔라피 가입 + 발신번호 등록** | 목요일 전 | SMS 즉시 가능해짐 |
| **카카오 알림톡 심사 신청** | 즉시 | 채널·발신프로필·템플릿 심사 1~2주 — 이번 주 시작 못 하면 SMS로 오픈 |
| PSD 전달 방식 결정 | 목요일 | 플랫폼 다운로드 vs 드라이브 링크 |
| AI 고지·동의 문구 | 금요일 | 의뢰서 반영 |

---

## 7. 리스크

- **네트워크 불안정** — 오늘 3회 발생(서브에이전트 인증 끊김·Gemini fetch 실패·플래너 연결 종료).
  재시도·폴백을 계속 보강 중이나 통제 불가 구간. 긴 실행은 여유를 두고 배치.
- **알림톡 심사 리드타임** — 이번 주 신청해도 오픈 시점에 미승인일 수 있음. SMS 폴백 전제로 설계.
- **폰트 계약 vs 디자인 다양성** — 타이틀 1종 강제로 변형별 세리프 타이틀을 포기했다.
  클라이언트가 다양성을 원하면 룰 자체를 조정해야 하므로 시연 때 확인 필요.
- **미배포 커밋 누적** — 로컬에 150건+. GitHub 백업이 필요하면 별도 요청.
