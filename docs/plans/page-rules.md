# 상세페이지 제작 글로벌 룰 v1.1 (2026-07-13 제정 · 2026-07-16 클라이언트 룰 편입)

배경: 프로덕션 검수에서 기초 오류 5건 재발(빈 원형·와이드 겹침·누끼 과대·좌측 공백·성분표 소형컷).
공통 근원은 개별 변형이 아니라 **전역 룰 부재** — ① 페이지 폭 표준 없음 ② 부분 결손 렌더 규칙이
변형마다 제각각 ③ 이미지 공급이 블록 선택의 종속변수(샷 수 제각각·활용 저조).
이 문서가 제작 룰의 단일 출처. 코드(렌더러·플래너·가드)는 이 문서 기준으로 정렬한다.

## 1. 페이지 캔버스 (전역 — 렌더러가 보장)
- **고정폭 872px, 중앙 정렬** — 변형이 아니라 페이지 스켈레톤이 강제(composer DEFAULT_WIDTH).
  커머스 업로드 규격(780~980) 내. vw→px 렌더 치환으로 와이드 창 붕괴 차단.
- 검증·시각 감사 뷰포트 = 산출물 폭과 동일(872). 와이드 뷰포트 의존 겹침 원천 차단.

## 2. 페이지 구성 (플래너 검증이 강제)
- 14~20블록. hero 시작, closing 종료. 동일 변형 2회 금지, 동일 아키타입 3연속 금지.
- 표계열(spec/faq/cs/shipping) 이미지 금지 (현행).
- **[신설] 이미지 보유 블록 하한**: 전체 블록의 50% 이상 이미지 슬롯 보유(슬롯 합 ≥ 10).
  청사진 검증에서 미달 시 재시도 — "16블록에 이미지 2장" 유형의 기획 단계 차단.

## 3. 표준 샷 패키지 (신설 — 샷 개수 표준화)
현행: 니즈 수 = 선택 블록의 슬롯 수(6~15 변동) → 제품마다 들쭉날쭉, 활용도 종속.
제안: **카테고리 공통 12컷 표준 패키지** — 블록 선택과 무관하게 공급 고정, 슬롯이 패키지를 소화.
| 용도 | 컷 수 | 티어 |
|---|---|---|
| 히어로 연출(withProduct) | 1 | main |
| 제품 연출 | 2 | main |
| 원료/소재 | 2 | support |
| 텍스처/디테일 | 2 | support |
| 사용 장면 | 2 | main 1 + support 1 |
| 무드/배경 | 1 | support |
| 스펙/비교 보조 | 1 | support |
| 예비(카테고리 갭) | 1 | support |
- 비용: main 4×$0.14 + support 8×$0.04 ≈ **$0.88/건 고정** (현행 변동 평균과 유사).
- 원본 사진은 패키지와 별도 풀(디테일·신뢰 슬롯 우선, hero·closing 금지 — 현행 가드 유지).
- 목표: 12컷 전량 사용, 미회수 0~2 허용(재배치+갤러리 소진).

## 4. 섹션별 이미지 룰 (아키타입 × 허용 소스 × 결손 처리)
| 아키타입 | 이미지 소스 | 결손 시 |
|---|---|---|
| hero | 연출 생성컷만(원본·단독 누끼 금지, 로고는 소형 슬롯만) | 페이지 실패 — 재생성 |
| story·gallery | 무드/라이프컷, 텍스트 스크림 필수 | 텍스트 강등 |
| ingredient | 원료·텍스처. 누끼전용 프레임=누끼만(현행 가드) | 리스트 전부-또는-0 |
| usage·feature 등 스텝/레일형 | 배열 아이템 이미지 **전부-또는-0** (아키타입 불문) | 아이콘/번호 강등 |
| detail·point·stats | 제품·디테일컷. 2컬럼 스플릿은 이미지 없으면 1컬럼 강등 | 1컬럼 강등 |
| compare | 좌우 각 1 또는 0 | 텍스트 표 강등 |
| spec·faq·cs·shipping | 금지 | — |
| closing | 생성 무드컷만(원본 금지 — 현행 가드) | 그라데이션 강등 |
| 공통(누끼) | 프레임 대비 최대 점유 상한 — 과대 확대·잘림 금지 | — |

## 5. 부분 결손 전역 규칙 (신설 — 기초 오류의 공통 근원 봉쇄)
- 어떤 변형도 **빈 이미지 플레이스홀더(.ph 등)를 최종 렌더에 노출하지 않는다** — 이미지가 없으면
  해당 요소를 숨기고 레이아웃을 재구성(noimg-safe 전수화).
- 렌더 후 결정적 검사: 플레이스홀더 노출 감지 시 해당 블록 드롭(가드 순서: 렌더 직전).
- 배열 아이템 균일화 가드를 usage/ingredient → **전 아키타입**으로 확대(이미지 키는
  mediaSlotKeys로 자동 식별 — 'image' 하드코딩 제거).

## 6. 활용·검증 (현행 유지 + 강화)
- 재배치 패스(니즈 홈→노트 매칭→배열 일괄), 오버레이형 주입 금지, 최소 밀도 min(6,후보).
- 게이트 체인: 태거 → 배치 가드 → 페어링 QA → 평가자(반려 재작업) → 시각 감사(폐루프) → 런 리포트.

## 7. 클라이언트 글로벌 룰 (2026-07-16 편입 — 회의 제안 + 확정 수치)

클라이언트 제시 룰셋을 시스템 언어로 정규화. 빈칸 수치는 우리 제안값(클라이언트 확정 대기),
문구 정교화 3건(7-2·7-8·7-10)은 충돌 방지 조정.

### 폰트 (v2 — 2026-07-20 클라이언트 확정 룰로 갱신)
사이즈는 872px 캔버스 기준 px(클라이언트 "포인트" 표기를 제작 캔버스 px로 해석 —
커머스 상세는 모바일에서 ~45%로 축소 표시되므로 웹 표준보다 큰 하한이 실무 기준).
| # | 룰 | 상태 |
|---|---|---|
| 7-1 | 폰트 최대 5종 | 토큰 체계 준수 — 화이트리스트 강제 예정 |
| 7-2 | 섹션 타이틀 1종 (사이즈 규정 없음 — 내부 기준 40~64px, 히어로 64~80px 유지) | 크기 분포 계측 중 |
| 7-3 | 서브 폰트 1종, **최소 30px** | 신규 하한 — 검사기 반영 예정(서브 분류 확립 후) |
| 7-4 | 기본 정보(본문) 폰트 1종, **최소 23px** | **검사기 반영 완료** — 기존 변형 대부분(14~17px) 위반: 폰트 스케일 상향 스프린트 필요 |
| 7-5 | 본문은 고딕 계열 | 현행(Noto Sans KR 계열) — 제공 폰트 중 SUIT가 후보 |
| 7-6 | 제공 폰트 내에서만 사용 | **타이틀 15계열 수령**(2026-07-20): SUIT·나눔스퀘어·나눔명조·마루부리·tvN 즐거운이야기·가나초콜릿·나눔브러쉬·부장님눈치체·잉크립퀴드·땅스부대찌개·문경감홍사과·빛고을광주·상주곶감·수성혜정. **서브·본문 폴더 미제공 — 클라이언트 확인 필요**. woff2 변환·self-host 파이프라인 예정 |
| 7-7 | 가독성: 본문 대비 4.5:1, 대형 3:1 이상, 이미지 위 텍스트 스크림 필수 | 현행 가드 + 감사 항목 |

### 섹션 (씬 구조)
| # | 룰 | 상태 |
|---|---|---|
| 7-8 | **7씬 고정**, 씬당 목표 2,000px(±20%), 상한 2,500px | 신규 — 씬 계층(블록 14~20개를 7씬에 그룹핑) |
| 7-9 | 전체 길이 ≤ 25,000px | 신규 — 검사기 |
| 7-10 | 씬 구분은 바탕색 교차로 시각화. 씬 내부 블록 리듬 120~180px, 씬 경계는 색 전환·디바이더·오버랩(여백 0 허용) | 신규 — "여백 150px 고정" 제안을 풀블리드 충돌 없게 조정 |

### 이미지
| # | 룰 | 상태 |
|---|---|---|
| 7-11 | 히어로 제품 잘림 금지 | 신규 — 샷 프롬프트 + 감사 항목 |
| 7-12 | 원료/성분 섹션 각각 사진 → **대표 2~4컷**(12컷 내 배분, 초과분 소형 티어 +$0.04/컷 허용) | 12컷 표준과 충돌 조정 |
| 7-13 | 단색 바탕 제품 단독컷은 누끼 | **기구현** (§4 누끼전용 가드) |
| 7-14 | 수치는 그래프 적극 사용, 기준치 대비 표기는 **입력 자료에 기준치가 있을 때만** (무근거 수치 가드 우선) | 조건부 편입 |
| 7-15 | 동일 사진 **최대 1회** ("2회 미만") — 클로징 배경 히어로 재사용 관행 폐지 | 신규 — 컴포저 집행 |
| 7-16 | 스타일링(연출) 사진 전량 사용 | **기구현** (12컷 표준 + 재배치 패스) |

### 기타
| # | 룰 | 상태 |
|---|---|---|
| 7-17 | 셀링포인트 홀수면 여백 없이 1열 나란히 (3개=한 줄 3칸) | 신규 — 포인트 그리드 규칙 |

## 구현 우선순위
- **P0 (기초 오류 직접 봉쇄)**: ① 페이지 고정폭 860 전역 ② 플레이스홀더 노출 금지(렌더 후 검사+드롭)
  ③ 배열 균일 가드 전 아키타입 확대 ④ 누끼 프레임 점유 상한
- **P1 (공급 표준화)**: 표준 12컷 패키지 + 청사진 이미지 블록 하한(§2·§3)
- **P2 (변형 정비)**: 2컬럼 강등, 성분표류 소형 슬롯 정리, 스텝 레일 노드 규칙
- **P3 (§7 클라이언트 룰 집행 — 2026-07-16)**: ① 씬 계층(7-8·7-10) ② 이미지 1회 사용(7-15)
  ③ 홀수 포인트 1열(7-17) ④ 히어로 잘림 금지(7-11) ⑤ 룰 검사기(7-2~4·7-9 — run-report 연동)

## 이번 검수 5건 → 룰 매핑
1. 루미트론 빈 원형(feature-checkpoint-rail, img 2/3) → §5 균일화 확대 + 플레이스홀더 금지
2. 동원 성분표 옆 소형컷 어색 → P2 슬롯 정리
3. 아로마티카 PRODUCT EFFECT 좌측 공백 → §4 2컬럼 강등
4. 로모노소프 선물 타이틀-이미지 겹침(1440px 재현) → §1 고정폭 — **최상위 원인, 타 건도 와이드에서 악화**
5. 매일 지그재그 누끼 과대·회전 잘림 → §4 누끼 점유 상한

## 8. 섹션별 룰 인벤토리 — 현행 강제 실태 (2026-07-20 감사 기준)

> 이 표는 §4에서 선언한 룰이 실제 코드에서 어떤 방식으로 집행되는지
> 파일 추적(감사 A·B·C, v7 런 ba59d5e1 기준)으로 확인한 결과다.
> **강제 방식** 3단계: **코드** (코드 가드 함수가 결정론적으로 집행) /
> **프롬프트** (LLM 지시문에만 의존, 생성 결과에 따라 달라짐) /
> **없음** (문서에 선언됐으나 코드·프롬프트 모두 미집행).

### 아키타입 × 이미지 룰 강제 실태

| 아키타입 | 허용 이미지 소스 | 결손 처리 | 강제 방식 | 집행 함수 / 위치 |
|---|---|---|---|---|
| **hero** | 연출 생성컷만 (원본·누끼 금지, 로고는 소형 슬롯만) | 페이지 실패→재생성 | 프롬프트 | `page-planner.ts` SYSTEM_PROMPT L162 (코드 가드 없음 — closing L1198 패턴과 비대칭) |
| **story · gallery** | 무드/라이프컷, 텍스트 스크림 필수 | 텍스트 강등 | 없음 | — |
| **ingredient** | 원료·텍스처; 누끼전용 프레임=누끼만 | 리스트 전부-또는-0 | 코드 | `applyPlacementGuards` L1187–1193 (`containSlotKeys` 자동 식별) |
| **usage · feature** 스텝/배열 | 배열 아이템 이미지 전부-또는-0 | 아이콘/번호 강등 | 코드 | `applyPlacementGuards` L1144–1161 (`mediaSlotKeys` 자동 식별) |
| **detail · point · stats** | 제품·디테일컷; 2컬럼 이미지 없으면 1컬럼 강등 | 1컬럼 강등 | 없음 | — (변형 noimg-safe 렌더 분기에만 위임) |
| **compare** | 좌우 각 1 또는 0 (누끼 OK — `CUTOUT_OK_ARCHETYPES`) | 텍스트 표 강등 | 없음 | — |
| **spec · faq · cs · shipping** | 금지 | — | 코드 | `TEXT_LED_ARCHETYPES` Set + `applyPlacementGuards` L1171 + `redistributeUnusedImages` `openSlots` L1439 |
| **cs** (로고 예외) | 로고 슬롯만 허용 | — | 코드 | `LOGO_OK_ARCHETYPES` Set |
| **closing** | 생성 무드컷만 (원본 금지) | 그라데이션 강등 | 코드 | `applyPlacementGuards` L1195–1201 (`/intake-files/` 차단) + `redistributeUnusedImages` L1477 재배치 금지 |
| **공통 — URL 1회 사용** | 동일 URL 2회+ 금지 | 2번째+ 슬롯 삭제 | 코드 | `applyPlacementGuards` L1122–1136 `urlCount` 맵 |
| **공통 — 누끼전용 슬롯** | 누끼(배경 없는 단독컷)만 | 슬롯 비움→noimg-safe 강등 | 코드 | `containSlotKeys` + `applyPlacementGuards` L1187 |
| **공통 — 이미지 블록 하한** | 슬롯 합 ≥ 10 (전체 50%+) | 재시도 | 코드 | `validateBlueprint` `page-planner.ts` L424–429 |
| **공통 — 빈 사진 블록 드롭** | imageSlots≥2 + 이미지 0장 → 드롭 (hero/closing 제외) | 블록 드롭 | 코드 | `dropEmptyPhotoBlocks` L1365–1388 |
| **공통 — 누끼 점유 상한** | 과대 확대·잘림 금지 (§4 P0 명시) | — | 없음 (P0 미구현) | — |

### 코드 강제 없는 룰 — 집행 공백 목록

문서에 선언됐으나 코드 가드가 없어 LLM 재량에 맡겨지는 항목.

| 룰 | 선언 위치 | 공백 내용 | 수정 우선순위 |
|---|---|---|---|
| hero 원본 코드 가드 | §4 + page-planner SYSTEM_PROMPT L162 | closing L1198 패턴과 비대칭 — 코드 레벨 차단 없음 | P1 |
| §7-11 히어로 잘림 금지 framing | §7-11 + `buildShotPrompt heroFramingBlock` | `e2e-service.ts:47` bypass로 `buildShotPrompt` 미실행 | P1 |
| 공통 누끼 점유 상한 | §4 P0 명시 | 코드 가드 완전 미구현 (매일 지그재그 실사례 반복) | P0 |
| §5 렌더 후 플레이스홀더 DOM 감지·드롭 | §5 | `dropEmptyPhotoBlocks`는 URL 0장 기준 사전 드롭; 실제 `.ph` DOM 감지 로직 없음 | P0 |
| detail·point·stats 2컬럼→1컬럼 강등 | §4 | 변형 noimg-safe에만 위임, 코드 강등 분기 없음 | P2 |
| story·gallery 텍스트 강등 | §4 | 코드 없음 | P2 |
| compare 텍스트 표 강등 | §4 | 코드 없음 | P2 |
| §7-17 홀수 포인트 1열 배치 | §7-17 | 코드 없음 | P3 |

> **참고** 7-15 "동일 이미지 최대 1회"는 `applyPlacementGuards` L1122 코드 집행 중.
> 7-16 "스타일링컷 전량 사용"은 `redistributeUnusedImages`가 존재하나
> 2026-07-20 감사(v7 런)에서 5개 실패 지점
> (① 플래너 imageNeeds 단절 ② 원본 5장 재배치 풀 제외 ③ 드롭 블록 고아화
> ④ PairingQA 3차 재제거 ⑤ openSlots 4중 조건 과제한)으로 유명무실 확인.
